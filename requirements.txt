discord.py-self @ git+https://github.com/dolfies/discord.py-self@2ba64a9a997e151a9c259984e0a179b1fdf4aff4
Flask==3.1.3
# psutil: process memory tracking for the multi-account memory budget
# (keeps too many selfbot accounts from OOM-killing the process on Render)
psutil>=5.9.0
requests==2.33.0
certifi>=2024.2.2
aiohttp==3.14.3
rich==14.2.0
plyer==2.1.0
playsound3==3.3.0
python-dotenv==1.2.2
aiohttp-socks==0.10.1
cryptography==50.0.0
# ── Dashboard login security (passkeys + 2FA) ──────────
# webauthn: WebAuthn/passkey server support (PyPI name is `webauthn`)
# pyotp: TOTP authenticator-app 2FA
# qrcode: renders the TOTP setup QR code (uses the already-present Pillow)
webauthn>=3.0.0
pyotp>=2.9.0
qrcode>=8.0
Pillow
# ── Voice support ─────────────────────────────────────────
# PyNaCl: required by discord.py-self's VoiceClient (declared voice extra).
davey
PyNaCl>=1.5.0,<1.6